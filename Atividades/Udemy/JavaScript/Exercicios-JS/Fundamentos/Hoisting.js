console.log(a);
var a = 1;  //aqui ocorre o hoisting (içamento, lançamento para cima). Caso essa linha seja apagada, ira gerar um erro ind.
            //não existe hoisting com let, apenas com var.
console.log(a);